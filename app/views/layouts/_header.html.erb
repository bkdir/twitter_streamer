<header class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <%= link_to "O_O", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li><%= link_to "Watch List", twitter_users_path %></il>
          <li><%= link_to "Deleted Twits", deleted_tweets_path %></il>
          <li><%= link_to "Users", users_path %></il>
          <li><%= link_to "Add User", add_user_path %></il>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Settings", edit_user_path(current_user), class: "dropdown-item" %></il>
              <li class="divider"></li>
              <li>
                <%= link_to "Log Out", logout_path, method: :delete, 
                  class: "dropdown-item"%>
               </il>
            </ul>
          </li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
